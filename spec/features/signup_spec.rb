#coding utf-8
require "rails_helper"

RSpec.describe 'Signup page', :feature do
  
  before do
    # visit 'users/sign_up'
  end

  describe 'allows visitor to register' do
    # let (:valid_user) { FactoryGirl.build(:user) }

    it 'with valid email and password' 
      # pending "Must be implemented later"

      # within("#new_user") do
      #   fill_in 'Email', :with => valid_user.email
      #   fill_in 'Password', :with => valid_user.password
      #   fill_in 'Password confirmation', :with => valid_user.password
      # end
      # click_button 'Sign up'
      # expect(current_path).to redirect_to(root_path)
      # expect(page).not_to have_content '2 errors'
    

  end
end